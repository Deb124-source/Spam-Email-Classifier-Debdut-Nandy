# TODO - Spam Email Classifier Project

- [ ] Inspect current `app.py`, `train_model.py`, and front-end files
- [ ] Implement `train_model.py`:
  - Train on `spam.csv`
  - Vectorize text
  - Save model + vectorizer to disk
  - Provide `get_model()` used by `app.py`
- [ ] Populate `requirements.txt` with Flask + scikit-learn dependencies
- [ ] Ensure `templates/index.html`, `static/style.css`, `static/script.js` are present/valid (minimal defaults if needed)
- [ ] Train model (run `python train_model.py`)
- [ ] Start Flask app and verify `/predict` endpoint with a quick local test
- [ ] Stop services / ensure artifacts exist

